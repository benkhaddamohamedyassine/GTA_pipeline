"""Pure-NumPy/OpenCV geometry: intrinsics, back-projection, crop-interior
centering, virtual camera placement, the vectorized z-buffer rasterizer, and
residual hole filling. Nothing in this package touches disk or a model --
those concerns live in ``io/``, ``models/``, and ``stages/``.
"""
