# Core (no models) -- needed for discovery, paths, manifest, geometry, config, tests.
numpy
opencv-python
Pillow
pandas
tqdm
matplotlib

# Models (Stage 2/5 depth, Stage 4 AI outpainting, Stage 12 post-warp super-resolution).
# See README "Colab setup" for the exact install order and the basicsr/torchvision
# compatibility note (handled automatically at runtime either way).
torch
torchvision
transformers
basicsr
realesrgan
simple-lama-inpainting

# Testing
pytest
